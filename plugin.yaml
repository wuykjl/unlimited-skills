# Hermes unlimited-skills plugin manifest
# Architecture: keyword match → LLM reason → user confirm
#   Phase 1: Keyword match against skills-catalog.md
#   Phase 2: LLM semantically reasons best fit from matched skills
#   Phase 3: User confirms to execute the skill
#
# Replaces the old 5-layer hybrid routing (BM25 + triggers + Levenshtein).
# Zero external dependencies. Single md file for catalog maintenance.

name: unlimited-skills
version: 2.0.0
description: "Keyword-match skill router: catalog → LLM reason → user confirm. Replaces BM25/triggers/Levenshtein with simple keyword match + AI semantic reasoning."
author: wuykjl
homepage: https://github.com/wuykjl/unlimited-skills
enabled: true
